services:
  web:
    build: .
    container_name: photoshare_web
    image: py-web-team-project-web:latest
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file: 
      - .env
    networks:
      - app_network
   
networks:
  app_network:
    driver: bridge